import { JsonDocsProp } from '../../../stencil-public-runtime';
export declare function PropertyList({ props, id, slugId, }: {
    id?: string;
    slugId?: string;
    props: Array<Partial<JsonDocsProp>>;
}): HTMLElement[];
