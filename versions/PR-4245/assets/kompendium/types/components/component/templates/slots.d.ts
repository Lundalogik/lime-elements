import { JsonDocsSlot } from '../../../stencil-public-runtime';
export declare function SlotList({ slots, id, slugId, }: {
    id?: string;
    slugId?: string;
    slots: JsonDocsSlot[];
}): HTMLElement[];
