import { JsonDocsEvent } from '../../../stencil-public-runtime';
export declare function EventList({ events, id, slugId, }: {
    id?: string;
    slugId?: string;
    events: JsonDocsEvent[];
}): HTMLElement[];
