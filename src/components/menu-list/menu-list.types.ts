/**
 * The type of the menu, omit to get a regular vertical menu.
 * Available types are:
 * `menu`: regular vertical menu.
 * `grid`: menu with menu items in several columns.
 */
export type MenuListType = 'menu' | 'grid';
