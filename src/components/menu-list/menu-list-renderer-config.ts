import { IconSize, MenuListType } from '../../interface';

export interface MenuListRendererConfig {
    isOpen?: boolean;
    badgeIcons?: boolean;
    type?: MenuListType;
    iconSize?: IconSize;
}
