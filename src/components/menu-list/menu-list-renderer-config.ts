import { IconSize, MenuListType } from '@limetech/lime-elements';

export interface MenuListRendererConfig {
    isMenu?: boolean;
    isOpen?: boolean;
    badgeIcons?: boolean;
    type?: MenuListType;
    iconSize?: IconSize;
}
