import { Component } from '@stencil/core';

@Component({
    tag: 'limel-spinner',
    styleUrl: 'spinner.less',
    shadow: true
})
export class Spinner {
    render() {
        return ('');
    }
}
