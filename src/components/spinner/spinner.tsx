import { Component } from '@stencil/core';

@Component({
    tag: 'limel-spinner',
    styleUrl: 'spinner.scss',
    shadow: true
})
export class Spinner {
    render() {
        return ('');
    }
}
