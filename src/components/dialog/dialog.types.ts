export interface DialogHeading {
    title: string;
    subtitle?: string;
    supportingText?: string;
    icon: string;
}

export interface ClosingActions {
    escapeKey: boolean;
    scrimClick: boolean;
}
